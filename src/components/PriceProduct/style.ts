import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  price: {
    fontWeight: "600",
    fontSize: 16,
    lineHeight: 20,
    color: "#212529",
  },
});

export { styles };
