import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontWeight: "600",
    fontSize: 20,
    lineHeight: 25,

    color: "#212529",
  },
});

export { styles };
