import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    paddingTop: 50,
    paddingBottom: 50,
    paddingLeft: 20,
  },
  subtitle: {
    fontWeight: "600",
    fontSize: 24,
    lineHeight: 30,
  },
  menu: {
    paddingTop: 50,
  },
});

export { styles };
