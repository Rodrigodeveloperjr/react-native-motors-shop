import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  description: {
    fontWeight: "400",
    fontSize: 14,
    lineHeight: 24,
    color: "#495057",
    height: 48,
  },
});

export { styles };
