import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontWeight: "600",
    fontSize: 16,
    lineHeight: 20,
    color: "#FFFFFF",
    paddingBottom: 20,
  },
});

export { styles };
