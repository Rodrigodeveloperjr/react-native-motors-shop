import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  image: {
    width: 312,
    height: 152,
    backgroundColor: "#E9ECEF",
    borderWidth: 2,
    borderColor: "#E9ECEF",
    resizeMode: "contain",
  },
});

export { styles };
