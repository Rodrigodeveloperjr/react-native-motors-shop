import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  price: {
    fontWeight: "600",
    fontSize: 16,
    lineHeight: 20,
    color: "#FFFFFF",
    paddingTop: 20,
  },
});

export { styles };
