import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  description: {
    fontWeight: "400",
    fontSize: 16,
    lineHeight: 28,
    color: "#CED4DA",
    height: 84,
  },
});

export { styles };
