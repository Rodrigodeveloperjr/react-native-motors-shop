import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontWeight: "600",
    fontSize: 16,
    lineHeight: 20,
    color: "#212529",
    paddingTop: 15,
    paddingBottom: 15,
  },
});

export { styles };
